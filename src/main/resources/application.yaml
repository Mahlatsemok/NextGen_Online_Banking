spring:
  application:
    name: NextGen_Online_Banking

  datasource:
    url: jdbc:h2:mem:nextgenbanking
    driver-class-name: org.h2.Driver
    username: sa
    password: 

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

jwt:
  secret: "NextGenBankingSecretKeyForJwtAuthentication123456789"
  expiration: 3600000
